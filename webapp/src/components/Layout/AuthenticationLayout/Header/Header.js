import React from 'react';
import PropTypes from 'prop-types';
import HeaderContent from '../../Homepage/Header/HeaderContent';

Header.propTypes = {};

function Header(props) {
  return <HeaderContent />;
}

export default Header;
